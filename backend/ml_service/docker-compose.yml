version: '3.8'

services:
  ml_service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chess_ml_service
    ports:
      - "8000:8000"
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - DEBUG=True
      - ENV=development
    restart: always
